-- Creates the moto_keybox database for local development.
-- Mounted into postgres via docker-entrypoint-initdb.d.
-- The default database (moto_club) is created by POSTGRES_DB env var.
CREATE DATABASE moto_keybox;
