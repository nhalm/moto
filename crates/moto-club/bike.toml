# bike.toml - Engine deployment configuration for moto-club
# See specs/moto-bike.md for full specification

name = "club"

[deploy]
replicas = 3
port = 8080

[health]
port = 8081
path = "/health/ready"

[resources]
cpu_request = "500m"
cpu_limit = "2"
memory_request = "512Mi"
memory_limit = "2Gi"
