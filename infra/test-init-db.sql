-- Test database initialization
-- Creates separate databases for each service (matching production setup)
-- Default database (POSTGRES_DB) is moto_test_club, this script creates moto_test_keybox

CREATE DATABASE moto_test_keybox;
